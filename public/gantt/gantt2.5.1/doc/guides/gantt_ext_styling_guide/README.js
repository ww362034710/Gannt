Ext.data.JsonP.gantt_ext_styling_guide({"guide":"<h1 id='gantt_ext_styling_guide-section-introduction-to-ext-templates'>Introduction to Ext Templates</h1>\n<div class='toc'>\n<p><strong>Contents</strong></p>\n<ol>\n<li><a href='#!/guide/gantt_ext_styling_guide-section-basic-information'>Basic information</a></li>\n<li><a href='#!/guide/gantt_ext_styling_guide-section-working-with-more-complex-datasets.'>Working with more complex datasets.</a></li>\n<li><a href='#!/guide/gantt_ext_styling_guide-section-selecting-html'>Selecting HTML</a></li>\n<li><a href='#!/guide/gantt_ext_styling_guide-section-applying-inline-styling'>Applying inline styling</a></li>\n<li><a href='#!/guide/gantt_ext_styling_guide-section-adding%2Fremoving-css-classes'>Adding/removing CSS classes</a></li>\n<li><a href='#!/guide/gantt_ext_styling_guide-section-using-ext.util.css'>Using Ext.util.CSS</a></li>\n</ol>\n</div>\n\n<p>Templates are fragments of HTML, that are used for dynamical creation of content. ExtJs has a two classes for templates, but we'll focus on <a href=\"http://docs.sencha.com/ext-js/4-1/#!/api/Ext.XTemplate\">XTemplate</a> as it's more advanced. In this section we'll see how templates might be used for customizing Ext Gantt up to user's needs.</p>\n\n<h2 id='gantt_ext_styling_guide-section-basic-information'>Basic information</h2>\n\n<p>The simplest usage of XTemplate looks as follows:</p>\n\n<pre><code>var tpl = new Ext.XTemplate('&lt;div&gt;Hello {text}&lt;/div&gt;');\n</code></pre>\n\n<p>The tpl variable is now a simple object, with HTML string. The <code>{text}</code> fragment is a placeholder for data. It'll be changed to any value sent in the data object under <code>text</code> parameter:</p>\n\n<pre><code>tpl.append(Ext.getBody(), {text: 'World!'});\n</code></pre>\n\n<p>which will generate us a simple <code>div</code> element with <code>Hello World!</code> text inside and render it to the body of the page. More details about the <code>append</code> function can be found <a href=\"http://docs.sencha.com/ext-js/4-1/#!/api/Ext.XTemplate-method-apply\">here</a>. If we don't want to attach our generated html to the DOM, we can use <code>apply</code> function which simply returns string with generated html :</p>\n\n<pre><code>tpl.apply({text: 'Wordl!'});\n</code></pre>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/tpl_apply.png\" alt=\"\" width=\"488\" height=\"183\"></p></p>\n\n<p>The above example was an entry-level example to the world of the XTemplate. A more advanced implementation is needed when you have to deal with a multidimensional dataset.</p>\n\n<h2 id='gantt_ext_styling_guide-section-working-with-more-complex-datasets.'>Working with more complex datasets.</h2>\n\n<p>Now let's try rendernig template with a more developed data structure. Let's say we have some worker's tasks list :</p>\n\n<pre><code>var data = { \n    workerName: 'John Smith', \n    department: 'QA', \n    tasks: [\n        {id: 1, name: 'Test 1', duration: '3h'},\n        {id: 2, name: 'Test 2', duration: '4h'},\n        {id: 3, name: 'Writing report', duration: '1h'}\n    ]\n};\n</code></pre>\n\n<p>First let's focus on rendering the tasks only. We will create a simple div element wrapping our tasks list. Next, we will use a subtemplate for auto-repeating the template block inside the tpl tag for each item in the tasks array. While looping through an array, a special variable <code>#</code> gives the current index ( take notice, that the index counter starts from 1 not 0).</p>\n\n<pre><code>var tpl = new Ext.XTemplate('&lt;div id=\"tasks-list\"&gt;',\n    '&lt;tpl for=\".\"&gt;',\n    '&lt;p&gt;No. : {#}. &lt;/p&gt;',\n    '&lt;p&gt;Name: {name}&lt;/p&gt;',\n    '&lt;p&gt;Duration: {duration}&lt;/p&gt;',\n    '&lt;p class=\"separator\"&gt;&lt;/p&gt;',\n    '&lt;/tpl&gt;',\n    '&lt;/div&gt;'\n);\n\ntpl.append(Ext.getBody(), data.tasks);\n</code></pre>\n\n<p>Everything apart of the <code>tasks</code> is skipped in the rendered html. But the subtemplate can examine more complicated data structures. For example, let's say we want all of the data from <code>data</code> object to be rendered. To achieve this we will call our <code>append</code> function in a different way and modify the template itself :</p>\n\n<pre><code>tpl.append(Ext.getBody(), data);\n</code></pre>\n\n<p>To inform the template generator, that we want to loop over <code>tasks</code> we will change the value assigned to <code>for</code> template operator :</p>\n\n<pre><code>var tpl = new Ext.XTemplate('&lt;div id=\"users-tasks\"&gt;',\n    '&lt;h1&gt;Name: {workerName}&lt;/h1&gt;',\n    '&lt;h2&gt;Department: {department&lt;/h2&gt;',\n    '&lt;h3&gt;Taksks: &lt;/h3&gt;',\n    '&lt;div id=\"tasks-list\"&gt;'\n    '&lt;tpl for=\"tasks\"&gt;',\n    '&lt;p&gt;No. : {#}. &lt;/p&gt;',\n    '&lt;p&gt;Name: {name}&lt;/p&gt;',\n    '&lt;p&gt;Duration: {duration}&lt;/p&gt;',\n    '&lt;p class=\"separator\"&gt;&lt;/p&gt;',\n    '&lt;/tpl&gt;',\n    '&lt;/div&gt;',\n    '&lt;/div&gt;'\n);\n</code></pre>\n\n<h1 id='gantt_ext_styling_guide-section-styling-with-ext'>Styling with Ext</h1>\n\n<p>As any other framework ExtJS API exposes methods helpful for selecting and styling html elements. In this section we will introduce and describe functions needed to select and customize part of the page.</p>\n\n<h2 id='gantt_ext_styling_guide-section-selecting-html'>Selecting HTML</h2>\n\n<p>In ExtJs we have several methods for selecting html elements from the DOM. They differ in expected parameters and returned value.Let's look at a sample page structure :</p>\n\n<pre><code>&lt;body&gt;\n    &lt;div id=\"wrapper\" class=\"foo\"&gt;\n        &lt;div class=\"left menu\"&gt;\n            &lt;p class=\"left\"&gt;Menu&lt;/p&gt;\n            &lt;ul&gt;\n                &lt;li class=\"first\"&gt;Menu 1&lt;/li&gt;\n                &lt;li&gt;Menu 2&lt;/li&gt;\n            &lt;/ul&gt;\n        &lt;/div&gt;\n    &lt;/div&gt;\n&lt;/body&gt;\n</code></pre>\n\n<h3 id='gantt_ext_styling_guide-section-ext.get'>Ext.get</h3>\n\n<p>Returns <a href=\"http://docs.sencha.com/ext-js/4-1/#!/api/Ext.dom.Element\">Ext.dom.Element</a> (objects which encapsulate DOM elements) when called with either id of the node, a DOM node itself or Ext.dom.Element. This method is similar to <code>getElementById</code>.</p>\n\n<p>Sample usage :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_get.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-ext.select'>Ext.select</h3>\n\n<p>Returns <a href=\"http://docs.sencha.com/ext-js/4-1/#!/api/Ext.dom.CompositeElement\">collection</a> of Ext.dom.Element when called with css selector (the selector should not be an id).</p>\n\n<p>Sample usage :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_select.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-ext.query'>Ext.query</h3>\n\n<p>Returns array of DOM nodes when called with css selector. This method is similar to <code>querySelectorAll</code>.</p>\n\n<p>Sample usage :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_query.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-ext.getdom'>Ext.getDom</h3>\n\n<p>Returns the actual DOM node when called with either id of the node, a DOM node itself or Ext.dom.Element.</p>\n\n<p>Sample usage :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_getdom.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h2 id='gantt_ext_styling_guide-section-applying-inline-styling'>Applying inline styling</h2>\n\n<p>We have already explained the basics of Cascading StyleS heets in the first part of the guide. If you don't know what CSS is, please start from there. Css styles can be applied to elements either in style declarations or by adding them directly to the element's html (inline style). The only way of adding inline styles we've shown till now was through different renderers. Now we'll take a look into other methods. All of the functions below are methods of Ext.dom.Element, so first let's select one :</p>\n\n<pre><code>var el = Ext.get('wrapper');\n</code></pre>\n\n<h3 id='gantt_ext_styling_guide-section-setstyle'>setStyle</h3>\n\n<p>Function used for setting different style attributes of an element. It can be invoked in two ways, with single style value :</p>\n\n<pre><code>el.setStyle('color', 'red');\n</code></pre>\n\n<p>or object with multiple parameters at once :</p>\n\n<pre><code>el.setStyle({\n    width  : 100,\n    height : 50,\n    color  : 'red'\n});\n</code></pre>\n\n<h3 id='gantt_ext_styling_guide-section-other-style-setters'>Other style setters</h3>\n\n<p>Ext introduces also specialized functions for setting particular css parameters :</p>\n\n<ul>\n<li><p>setWidth - as name suggests this function changes width of the element. It accepts number value :</p>\n\n<p>  el.setWidth(100);</p></li>\n<li><p>setHeight - as name suggests this function changes height of the element. It accepts number value :</p>\n\n<p>  el.setHeight(100);</p></li>\n<li><p>setLeft - changes left position of the element. It accepts string value :</p>\n\n<p>  el.setLeft('100px');</p></li>\n<li><p>setRight - changes right position of the element. It accepts string value :</p>\n\n<p>  el.setRight('100px');</p></li>\n<li><p>setTop - changes top position of the element. It accepts string value :</p>\n\n<p>  el.setTop('100px');</p></li>\n<li><p>setBottom - changes bottom position of the element. It accepts string value :</p>\n\n<p>  el.setBottom('100px');</p></li>\n</ul>\n\n\n<h2 id='gantt_ext_styling_guide-section-adding%2Fremoving-css-classes'>Adding/removing CSS classes</h2>\n\n<p>Not always using inline styles is allowed/recommended (for example adding many style settings to one element make the html code less readable). In cases like that, we stick to applying css rules to selectors. Classes are one of the dom selectors, which can appear many times on the same page (in contrary to ID's which should be unique) and can be added to any dom elements. Ext gives us few intuitive methods to control classes, which once again belong to Ext.dom.Element. Let's select some element first :</p>\n\n<pre><code>var el = Ext.get('wrapper');\n</code></pre>\n\n<p>This is the initial view on our page structure :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/css_initial.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-addcls'>addCls</h3>\n\n<p>Method for adding a new CSS class to the element. Accepts string with class name.</p>\n\n<pre><code>el.addCls('baz');\n</code></pre>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_addcls.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-removecls'>removeCls</h3>\n\n<p>Method for removing CSS class. If it's not found, nothing changes. Accepts string with class name.</p>\n\n<pre><code>el.removeCls('foo');\nel.removeCls('bar');\n</code></pre>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_removecls.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-replacecls'>replaceCls</h3>\n\n<p>Replaces a CSS class on the element with another. If the old name does not exist, the new name will be added. Accepts strings with old and new names.</p>\n\n<pre><code>el.replaceCls('foo', 'bar');\nel.replaceCls('baz', 'bar1');\n</code></pre>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_replacecls.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-togglecls'>toggleCls</h3>\n\n<p>Toggles the specified CSS class on this element (removes it if it already exists, otherwise adds it). Accepts string with class name.</p>\n\n<pre><code>el.toggleCls('foo');\n</code></pre>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_removecls.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h2 id='gantt_ext_styling_guide-section-using-ext.util.css'>Using Ext.util.CSS</h2>\n\n<p>Another way of manipulating css rules in Ext is using the <a href=\"http://docs.sencha.com/ext-js/4-1/#!/api/Ext.util.CSS\">Css util</a> class. We'll describe in a nutshell the most useful methods given in this class. Let's add a simple stylesheet to head of our previously used page :</p>\n\n<pre><code>&lt;style&gt;\n    #wrapper {\n        color: black;\n    }\n&lt;/style&gt;\n</code></pre>\n\n<p>Here's the initial view of the webpage :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_css_initial.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-createstylesheet'>createStyleSheet</h3>\n\n<p>Creates a stylesheet DOM element from text with css rules and appends it to the head of the page.</p>\n\n<pre><code>Ext.util.CSS.createStyleSheet('#wrapper{border: 1px solid black;}');\n</code></pre>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_createstylesheet.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_createstylesheet2.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-removestylesheet'>removeStyleSheet</h3>\n\n<p>Removes a stylesheet or link tag when called with element's ID.</p>\n\n<pre><code>Ext.util.CSS.removeStyleSheet('style1');\n</code></pre>\n\n<h3 id='gantt_ext_styling_guide-section-getrule'>getRule</h3>\n\n<p>Returns css rules by matching selectors. Accepts string or array of strings with css selectors. For example the code below :</p>\n\n<pre><code>Ext.util.CSS.getRule('#wrapper');\n</code></pre>\n\n<p>will return us rules defined for <code>#wrapper</code> selector.</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_getrule1.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/ext_getrule2.png\" alt=\"\" width=\"543\" height=\"308\"></p></p>\n\n<h3 id='gantt_ext_styling_guide-section-updaterule'>updateRule</h3>\n\n<p>Updates css rules for matching selectors. Accepts string or array of strings with css selectors, string with css property and string with this property's value. For example code below :</p>\n\n<pre><code>Ext.util.CSS.updateRule('#wrapper', 'color', 'red');\n</code></pre>\n\n<p>will overwrite <code>color: black</code> for <code>#wrapper</code> selector</p>\n\n<h1 id='gantt_ext_styling_guide-section-using-column-renderers'>Using column renderers</h1>\n\n<p>Column <a href=\"http://docs.sencha.com/ext-js/4-1/#!/api/Ext.grid.column.Column-cfg-renderer\">renderers</a> are a specialized functions called for each cell in the grid while rendering. This gives us control of the look of single table cells. The code below shows a simple css style declaration for <code>sch-odd-row</code> class, and a columns definition for Gantt panel. The renderer function shown here creates a \"zebra\" effect in the locked grid of Ext Gantt by adding previously defined css class to <code>td</code> element of the cell :</p>\n\n<pre><code>.sch-ganttpanel .sch-odd-row{\n    background-color: #EBEBEB;\n}\n\ncolumns : [\n    {\n        xtype : 'treecolumn',\n        header: 'Tasks',\n        sortable: true,\n        dataIndex: 'Name',\n        width: 200,\n        renderer : function(v, meta, r, rIndex) {\n\n            //if index divides by two, add css class\n            if (!(rIndex % 2)) meta.tdCls = 'sch-odd-row';\n\n            return v;\n        }\n    },\n</code></pre>\n\n<p>And this is how our column looks after rendering :</p>\n\n<p><p><img src=\"guides/gantt_ext_styling_guide/grid_renderer.png\" alt=\"\" width=\"254\" height=\"263\"></p></p>\n","title":"Using Ext functionality for styling and customizing Ext Gantt 2"});