(function(window, $) {
    // 值组件  input datebox numberbox combobox combotree combotreegrid combogrid
    var ValueFeature = new Class({

    });

    window.ValueFeature = ValueFeature;
})(window, jQuery);